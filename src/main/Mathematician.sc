def square(x: Int): Int = x * x

square(2)

// Types
val one: Int = 1 // defined explicitly
val uno = 1 // inferred implicitly (type inference)

val gibberish: String = "$Q^HQ%	$T	%Y%GREVE$$"

val t: Boolean = true
